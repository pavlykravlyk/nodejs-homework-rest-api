const { contactSchema, favoriteSchema } = require("./contact");
const { registerSchema, loginSchema } = require("./user");

module.exports = { contactSchema, favoriteSchema, registerSchema, loginSchema };
